info:
  name: LakePut
  type: http
  seq: 2

http:
  method: PUT
  url: "{{GitCloudAddress}}/lake/:path?root=/"
  params:
    - name: root
      value: /
      type: query
    - name: path
      value: Lake/test
      type: path
  body:
    type: text
    data: dGVzdA==
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
