info:
  name: LakeGet
  type: http
  seq: 1

http:
  method: GET
  url: "{{GitCloudAddress}}/lake/{{path}}"
  auth: inherit

runtime:
  variables:
    - name: path
      value: Lake/test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
