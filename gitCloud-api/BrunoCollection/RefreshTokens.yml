info:
  name: RefreshTokens
  type: http
  seq: 5

http:
  method: GET
  url: "{{GitCloudAddress}}/refreshtokens"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
