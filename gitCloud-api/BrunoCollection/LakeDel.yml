info:
  name: LakeDel
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{GitCloudAddress}}/lake/{{path}}"
  auth: inherit

runtime:
  variables:
    - name: path
      value: Lake/test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
