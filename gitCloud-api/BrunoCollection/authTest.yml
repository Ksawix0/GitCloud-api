info:
  name: authTest
  type: http
  seq: 5

http:
  method: GET
  url: "{{GitCloudAddress}}/authTest"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: req.setHeader("Authorization", bru.getVar("AccesToken"))

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
