info:
  name: authTest
  type: http
  seq: 5

http:
  method: GET
  url: "{{GitCloudAddress}}/authTest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
